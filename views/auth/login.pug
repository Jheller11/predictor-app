extends ../layout.pug

block content
    .card
        .card-body
            h4.text-orange Log In:
            form(method="POST" action='/users/login')
                .form-group
                    label(for='email') Email Address:  
                    input.border-orange-focus.form-control(type='text' placeholder='jdoe@gmail.com' name='email' required)
                .form-group
                    label(for='password') Password:
                    input.border-orange-focus.form-control(type='password' name='password' required)
                button.btn.btn-secondary(type="submit") Log In
            div Don't have an account yet? 
                a(href="/users/signup") Create one here.